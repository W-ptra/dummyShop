// function Truncet(text: string, max: number, min:number): string {
//     const maxLength = screenWidth < 768 ? min : max;
//     return text.length > maxLength ? text.slice(0, maxLength) + "..." : text;
// }